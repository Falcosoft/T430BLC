# OPREGPM

Intel OpRegion protected-mode diagnostic.

## Build

Run:

```dos
BUILD
```

The build script supplies the repository `include` directory to TASM.

See the main T430LCD documentation for usage, supported hardware, and safety notes.
